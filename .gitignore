# Same front-end that's polymorphic to different environments
cachedResData.json
cachedResPartial.html

# Pre-compressed variants of the cached files (built by cache_compress.js).
# Regenerated on every `npm run build-*`, so never tracked.
cachedRes*.br
cachedRes*.gz

# Same front-end that's polymorphic to different note collections
env/description-meta.php
env/description-must.php
env/description.php
env/dir-snippets.php
env/icons.js
env/title-long.php
env/title.php
env/url-update-log.php

# Backups
backups/

+saveid*
.DS_Store
thumbs.db
desktop.ini
.vscode
package-lock.json
node_modules/
.sass-cache/
*.css.map
out/
*.env
.env-password.php
env/all/pcregrep.php
**/*(SUPERPRIVATE)*.md

# Rate-limit counter files (one JSON per IP hash). See README - Throttle Note Requests.md.
temp/throttle/